// import * as remote from '@electron/remote';

// export default async function getDir(): Promise<string | null> {
//   const { canceled, filePaths } = await remote.dialog.showOpenDialog({
//     properties: ['openDirectory'],
//   });
//   if (canceled) {
//     return null;
//   }
//   return filePaths[0];
// }
