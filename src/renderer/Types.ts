type Model = {
  id: number;
  uid: string;
  name: string;
  version: string;
  author: string;
  lastUpdated: string;
  input: string;
  inputTypes: string;
  outputTypes: string;
  parameters: string;
  constraints: string;
  ip: string;
  status: string;
};

export type { Model };
