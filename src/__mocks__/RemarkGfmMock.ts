import { Options } from 'remark-gfm';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function remarkGfmMock(options?: Options | null | undefined) {
  return undefined;
}
